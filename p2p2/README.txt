1. Open data.txt
2. Enter inputs separated by spaces
3. Compile program with compiler
	g++ main.cpp -o output
4. Run program
	./output
5. Observe output. The console will print the indices at which the subarray exist and will print out the subarray.